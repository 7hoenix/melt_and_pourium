<%= stylesheet_link_tag 'heroic-features', media: 'all', 'data-turbolinks-track' => true %>

  <!-- Page Content -->

      <!-- Jumbotron Header -->
      <header class="jumbotron hero-spacer">
          <h1>All Items</h1>
          <p>
            <%= link_to "View by category", categories_path, class: "btn btn-primary" %>
          </p>
          <% if current_user && current_user.admin? %>
            <p><%= link_to "Add Item", new_admin_category_item_path(@category) %></p>
          <% end %>
      </header>

      <hr>
      <!-- Title -->

      <!-- Items -->
      <%= render partial: "item", collection: @items %>
